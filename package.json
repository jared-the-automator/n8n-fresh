{
  "name": "n8n",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@types/express": "4.17.13",
    "@vitejs/plugin-vue": "4.6.2",
    "typescript": "5.3.3"
  },
  "dependencies": {
    "n8n-workflow": "1.21.0"
  },
  "scripts": {
    "build": "cd packages/nodes-base && npm install && npm run build && cd ../editor-ui && npm install && npm run build"
  }
}
